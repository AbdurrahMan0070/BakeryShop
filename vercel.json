{
  "version": 2,
  "buildCommand": "cd bakery-backend && npm install && npx prisma generate && npm run build && cd ../bakery-frontend && npm install && npm run build",
  "outputDirectory": "bakery-frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}
